plugins {
    id 'java'
}

sourceCompatibility = 1.8

repositories {
    jcenter()
    mavenCentral()
}

dependencies {
    compile project(':proteus-spring-connector')

    compile 'org.springframework:spring-context:5.0.7.RELEASE'
    compile 'org.springframework:spring-webflux:5.0.7.RELEASE'
}
